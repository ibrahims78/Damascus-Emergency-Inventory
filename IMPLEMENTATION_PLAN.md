# خطة التنفيذ التفصيلية — نظام مستودع الإسعاف والطوارئ
**الإصدار:** 1.0  
**تاريخ الإنشاء:** 3 أغسطس 2026  
**المرجع:** خطة_تنفيذ_نظام_مستودع_الإسعاف.md (النسخة 2.0)

---

## نظرة عامة

هذا الملف يُوثّق خطة تنفيذ جميع النواقص في التطبيق على شكل **مراحل مستقلة** قابلة للاختبار والتسليم. كل مرحلة تُنفَّذ بالكامل، تُختبر، ثم تُوثَّق هنا قبل الانتقال للمرحلة التالية.

### الحالة الراهنة (قبل البدء)

| الطبقة | النسبة | الملاحظة |
|---|---|---|
| Backend (API) | 95% | مكتمل — جميع الـ endpoints جاهزة |
| قاعدة البيانات | 85% | 8/11 جداول — ينقص audit_log، backup_log |
| Frontend | 50% | 4 صفحات رئيسية فارغة تماماً |
| السندات/الطباعة | 10% | API جاهز، الواجهة stub فارغ |
| التقارير | 5% | API جاهز، الواجهة فارغة |

### ترتيب الأولويات

```
المرحلة 1 → العمليات (إدخال/إخراج) + السند       ← الأكثر أهمية وظيفياً
المرحلة 2 → صفحة التقارير                        ← تحليل وإدارة المخزون
المرحلة 3 → إدارة المستخدمين                     ← التحكم بالصلاحيات
المرحلة 4 → الإعدادات + كلمة المرور              ← ضرورة للنشر الفعلي
المرحلة 5 → نظام التنبيهات في الـ Header          ← تجربة مستخدم
المرحلة 6 → جدول Audit Log + استيراد Excel       ← تتبع العمليات
المرحلة 7 → مراجعة شاملة + صقل نهائي + نشر       ← جودة ونشر
```

---

## المرحلة 1 — واجهة العمليات والسند القابل للطباعة

**الأولوية:** 🔴 عالية جداً  
**الحالة:** ✅ مكتملة — 3 أغسطس 2026  
**التعقيد:** كبير  
**الوقت المتوقع:** جلسة واحدة

### الوصف
هذه المرحلة هي محور النظام — بدونها لا يمكن تسجيل أي حركة مخزون. تشمل:
1. **صفحة سجل العمليات** `/transactions` — جدول بجميع الحركات مع فلاتر
2. **نموذج إدخال مادة** `/transactions/in/new` — إضافة مواد للمستودع
3. **نموذج إخراج مادة** `/transactions/out/new` — صرف مواد من المستودع
4. **السند القابل للطباعة** `/print/:id` — A4 بشعار المنظومة وتوقيع

### المتطلبات التفصيلية

#### 1.1 صفحة سجل العمليات (`transactions.tsx`)
- جدول يعرض: رقم السند، التاريخ، النوع (إدخال/إخراج)، الصنف، الكمية، الجهة، المستخدم
- فلاتر: نوع العملية (الكل/إدخال/إخراج)، نطاق التاريخ، نوع الصنف (مادة/تجهيز)
- Pagination (50 عملية في الصفحة)
- زران: "إدخال مادة" و"إخراج مادة"
- كل صف يحتوي رابط لعرض/طباعة السند

#### 1.2 نموذج الإدخال (`transactions/in/new`)
- اختيار نوع الصنف: مادة أو تجهيز
- حقل بحث واختيار الصنف من القائمة
- الكمية (للمواد) أو حالة التجهيز الجديدة
- رقم المستند (اختياري)
- ملاحظات (اختياري)
- عرض الرصيد الحالي بجانب الصنف المختار
- confirmation قبل الحفظ

#### 1.3 نموذج الإخراج (`transactions/out/new`)
- اختيار نوع الصنف: مادة أو تجهيز
- حقل بحث واختيار الصنف
- الكمية (تحقق: لا تتجاوز الرصيد المتاح)
- اختيار الجهة المستلمة (قائمة recipients + حقل اسم المستلم)
- سبب الإخراج (قائمة exit_reasons)
- رقم المستند، ملاحظات
- تحذير إذا كانت الكمية ستجعل الرصيد أقل من الحد الأدنى

#### 1.4 السند القابل للطباعة (`print-transaction.tsx`)
- تنسيق A4 — RTL كامل
- رأس السند: شعار المنظومة + اسمها الرسمي
- نوع العملية (إدخال/إخراج) + رقم السند + التاريخ
- جدول: الصنف، الوحدة، الكمية، الملاحظات
- بيانات الجهة المستلمة + اسم المستلم (للإخراج)
- سبب الإخراج (للإخراج)
- توقيع أمين المستودع + توقيع المستلم (خانات فارغة)
- زر "طباعة" (window.print) + زر "تحميل PDF"
- CSS للطباعة: إخفاء الأزرار عند الطباعة الفعلية

### معايير القبول (Definition of Done)
- [x] تظهر قائمة العمليات بجدول يعمل مع Pagination
- [x] نموذج الإدخال يحفظ عملية ويحدّث الرصيد فوراً
- [x] نموذج الإخراج يتحقق من الكمية ولا يسمح بما يتجاوز الرصيد
- [x] السند يُطبع بتنسيق A4 نظيف بدون عناصر الواجهة
- [x] جميع النصوص بالعربية RTL
- [x] يعمل في Dark Mode وLight Mode
- [x] الـ TypeScript typecheck يمر بدون أخطاء

### الـ APIs المستخدمة (موجودة ومكتملة)
```
GET  /api/transactions          ← قائمة العمليات مع فلترة وpagination
POST /api/transactions/in       ← إضافة عملية إدخال
POST /api/transactions/out      ← إضافة عملية إخراج
GET  /api/transactions/:id      ← تفاصيل عملية واحدة
GET  /api/transactions/:id/print ← بيانات السند
GET  /api/items                 ← لقائمة الأصناف في النموذج
GET  /api/equipment             ← لقائمة التجهيزات في النموذج
GET  /api/recipients            ← الجهات المستلمة
GET  /api/exit-reasons          ← أسباب الإخراج
```

### الملفات المنفَّذة
```
artifacts/web/src/pages/transactions.tsx          340 سطر — سجل العمليات + فلاتر + pagination
artifacts/web/src/pages/transaction-in-form.tsx   438 سطر — نموذج الإدخال + confirmation
artifacts/web/src/pages/transaction-out-form.tsx  543 سطر — نموذج الإخراج + تحقق الرصيد + تحذيرات
artifacts/web/src/pages/print-transaction.tsx     354 سطر — سند A4 RTL + طباعة + توقيعات
```

### نتائج الاختبار

| الاختبار | النتيجة | ملاحظة |
|---|---|---|
| عرض قائمة العمليات | ✅ | جدول + فلاتر + pagination تعمل |
| نموذج إدخال مادة | ✅ | بحث + رصيد حالي + confirmation + تحويل للسند |
| نموذج إدخال تجهيز | ✅ | قائمة التجهيزات + حالة المعدة |
| نموذج إخراج مادة | ✅ | جهة مستلمة + سبب + تحقق الرصيد |
| منع إخراج يتجاوز الرصيد | ✅ | Hard block + رسالة خطأ واضحة |
| تحذير ما دون الحد الأدنى | ✅ | تحذير مرئي قبل التأكيد |
| سند A4 قابل للطباعة | ✅ | RTL + شعار المنظومة + توقيعات + print CSS |
| TypeScript typecheck | ✅ | `pnpm typecheck` يمر بدون أخطاء |
| كلا الـ Workflows | ✅ | api-server:8080 + web:22333 يعملان |

### توثيق التنفيذ
**3 أغسطس 2026** — المرحلة كانت منفَّذة جزئياً في جلسة سابقة. تم في هذه الجلسة:
- تثبيت الـ dependencies (`pnpm install`)
- دفع الـ schema لقاعدة البيانات (`drizzle-kit push`)
- تشغيل الـ seed (admin + categories + recipients + exit_reasons)
- بناء مكتبات الـ TypeScript (`tsc --build`)
- إصلاح 27 خطأ TypeScript (`noImplicitAny`) في 6 ملفات صفحات
- التحقق من نجاح `pnpm --filter @workspace/web run typecheck`

---

## المرحلة 2 — صفحة التقارير الشاملة

**الأولوية:** 🔴 عالية  
**الحالة:** ⏳ في الانتظار  
**التعقيد:** متوسط-كبير  
**الوقت المتوقع:** جلسة واحدة

### الوصف
تنفيذ صفحة التقارير التي تُمكّن من استعراض وتصدير بيانات المستودع. الـ API يعيد كل البيانات، الواجهة فارغة.

### المتطلبات التفصيلية

#### 2.1 تبويبات التقارير (Tabs)
- **تبويب 1: جرد المخزون** — جميع الأصناف مع رصيدها الحالي والحد الأدنى وتاريخ الانتهاء
- **تبويب 2: حركة المواد** — فلتر بالتاريخ والنوع (إدخال/إخراج/الكل)، جدول قابل للتصدير
- **تبويب 3: قرب انتهاء الصلاحية** — أصناف تنتهي خلال 60 يوماً مرتبة تصاعدياً
- **تبويب 4: أقل من الحد الأدنى** — أصناف تحتاج طلبية عاجلة مع الفرق
- **تبويب 5: حالة التجهيزات** — جدول بكل التجهيزات وحالتها وحاملها

#### 2.2 فلاتر التقارير
- نطاق التاريخ (من - إلى) — للتقارير التي تدعمها
- فلتر التصنيف
- زر "إعادة ضبط الفلاتر"

#### 2.3 تصدير البيانات
- زر "طباعة" لكل تبويب — يُخفي الـ Header/Sidebar ويطبع الجدول فقط
- زر "تصدير CSV" (اختياري — يُنشئ ملف CSV في المتصفح)

#### 2.4 الإحصائيات المختصرة
- بطاقة ملخص في أعلى كل تبويب (عدد الأصناف، مجموع الكميات، إلخ)

### معايير القبول
- [ ] 5 تبويبات تعمل وتجلب البيانات من الـ API
- [ ] فلاتر التاريخ تعمل على تبويب "حركة المواد"
- [ ] جميع الجداول تعرض رسالة "لا توجد بيانات" عند الفراغ
- [ ] طباعة كل تبويب تعمل بدون عناصر التنقل
- [ ] TypeScript typecheck يمر بدون أخطاء

### الـ APIs المستخدمة
```
GET /api/reports/stock       ← جرد المخزون
GET /api/reports/movements   ← حركة المواد (يقبل: from، to، type)
GET /api/reports/expiry      ← قرب انتهاء الصلاحية
GET /api/reports/below-min   ← أقل من الحد الأدنى
GET /api/reports/equipment   ← حالة التجهيزات
```

### الملفات التي ستُعدَّل
```
artifacts/web/src/pages/reports.tsx   ← تنفيذ كامل (حالياً 0 سطر)
```

### نتائج الاختبار
> *تُملأ بعد التنفيذ*

| الاختبار | النتيجة | ملاحظة |
|---|---|---|
| عرض تبويب جرد المخزون | — | |
| فلتر التاريخ على حركة المواد | — | |
| قرب انتهاء الصلاحية يعرض النتائج | — | |
| طباعة تقرير | — | |
| TypeScript typecheck | — | |

### توثيق التنفيذ
> *يُضاف بعد الانتهاء*

---

## المرحلة 3 — إدارة المستخدمين

**الأولوية:** 🟡 متوسطة-عالية  
**الحالة:** ⏳ في الانتظار  
**التعقيد:** متوسط  
**الوقت المتوقع:** جلسة واحدة

### الوصف
تنفيذ صفحة إدارة المستخدمين المتاحة للمدير فقط. الـ API كامل ومكتمل.

### المتطلبات التفصيلية

#### 3.1 قائمة المستخدمين
- جدول: الاسم الكامل، اسم المستخدم، الدور، الحالة (نشط/معطل)، تاريخ الإنشاء
- رمز لوني للأدوار (admin، warehouse_manager، viewer)
- زر "مستخدم جديد"
- أزرار: تعديل، تفعيل/تعطيل، حذف (مع تأكيد)
- المستخدم الحالي لا يمكن حذفه (الـ API يمنع ذلك)

#### 3.2 نموذج إضافة/تعديل مستخدم (Dialog)
- الاسم الكامل (مطلوب)
- اسم المستخدم (مطلوب، عند الإضافة فقط)
- كلمة المرور (مطلوبة عند الإضافة، اختيارية عند التعديل)
- الدور: admin / warehouse_manager / viewer مع وصف لكل دور
- حالة الحساب (نشط/معطل)

#### 3.3 صلاحيات العرض
- الصفحة مرئية فقط للـ admin
- المستخدمون بصلاحية viewer/warehouse_manager يُحوَّلون للرئيسية

### معايير القبول
- [ ] عرض قائمة المستخدمين
- [ ] إضافة مستخدم جديد يعمل
- [ ] تعديل بيانات مستخدم يعمل
- [ ] تعطيل/تفعيل مستخدم يعمل
- [ ] حذف مستخدم (مع تأكيد) يعمل
- [ ] المستخدم غير admin لا يرى الصفحة
- [ ] TypeScript typecheck يمر

### الـ APIs المستخدمة
```
GET    /api/users        ← قائمة المستخدمين
POST   /api/users        ← إضافة مستخدم
PUT    /api/users/:id    ← تعديل مستخدم
DELETE /api/users/:id    ← حذف مستخدم (soft delete)
```

### الملفات التي ستُعدَّل
```
artifacts/web/src/pages/users.tsx   ← تنفيذ كامل (حالياً stub)
```

### نتائج الاختبار
> *تُملأ بعد التنفيذ*

| الاختبار | النتيجة | ملاحظة |
|---|---|---|
| عرض قائمة المستخدمين | — | |
| إضافة مستخدم | — | |
| تعديل مستخدم | — | |
| تعطيل/تفعيل | — | |
| الحماية (viewer لا يرى الصفحة) | — | |
| TypeScript typecheck | — | |

### توثيق التنفيذ
> *يُضاف بعد الانتهاء*

---

## المرحلة 4 — صفحة الإعدادات

**الأولوية:** 🟡 متوسطة  
**الحالة:** ⏳ في الانتظار  
**التعقيد:** صغير  
**الوقت المتوقع:** نصف جلسة

### الوصف
تنفيذ صفحة الإعدادات لتغيير معلومات المنظومة وكلمة المرور. الـ API موجود.

### المتطلبات التفصيلية

#### 4.1 إعدادات المنظومة (admin فقط)
- اسم المنظومة الرسمي على السندات
- عدد الأيام للتنبيه قبل انتهاء الصلاحية (افتراضي: 30 يوماً)
- زر حفظ مع رسالة نجاح

#### 4.2 الملف الشخصي (لكل المستخدمين)
- الاسم الكامل (قابل للتعديل)
- اسم المستخدم (للعرض فقط — غير قابل للتغيير)
- الدور الحالي (للعرض فقط)

#### 4.3 تغيير كلمة المرور
- كلمة المرور الحالية
- كلمة المرور الجديدة
- تأكيد كلمة المرور الجديدة
- التحقق من تطابق كلمتَي المرور

### معايير القبول
- [ ] تحميل الإعدادات الحالية وعرضها
- [ ] تحديث إعدادات المنظومة يعمل
- [ ] تغيير كلمة المرور يعمل
- [ ] رسائل خطأ مناسبة عند كلمة المرور الخاطئة
- [ ] TypeScript typecheck يمر

### الـ APIs المستخدمة
```
GET /api/settings                ← جلب الإعدادات الحالية
PUT /api/settings                ← تحديث إعدادات المنظومة
POST /api/settings/change-password ← تغيير كلمة المرور
```

### الملفات التي ستُعدَّل
```
artifacts/web/src/pages/settings.tsx   ← تنفيذ كامل (حالياً stub)
```

### نتائج الاختبار
> *تُملأ بعد التنفيذ*

| الاختبار | النتيجة | ملاحظة |
|---|---|---|
| تحميل الإعدادات الحالية | — | |
| تعديل إعدادات المنظومة | — | |
| تغيير كلمة المرور بنجاح | — | |
| رفض كلمة المرور الخاطئة | — | |
| TypeScript typecheck | — | |

### توثيق التنفيذ
> *يُضاف بعد الانتهاء*

---

## المرحلة 5 — نظام التنبيهات في الـ Header

**الأولوية:** 🟡 متوسطة  
**الحالة:** ⏳ في الانتظار  
**التعقيد:** صغير  
**الوقت المتوقع:** ربع جلسة

### الوصف
ربط الـ `GET /api/alerts` بجرس التنبيهات في الـ Header لإظهار التنبيهات النشطة (نواقص مخزون، قرب انتهاء صلاحية، تجهيزات تحتاج صيانة).

### المتطلبات التفصيلية

#### 5.1 جرس التنبيهات في الـ Header
- أيقونة جرس مع رقم (badge) يظهر عدد التنبيهات النشطة
- اللون أحمر عند وجود تنبيهات حرجة (critical)، برتقالي للتحذيرات (warning)
- عدم ظهور الرقم عند لا توجد تنبيهات

#### 5.2 قائمة التنبيهات المنسدلة (Dropdown)
- تُفتح بالضغط على الجرس
- كل تنبيه يعرض: أيقونة النوع، الرسالة، مستوى الخطورة
- روابط مباشرة لصفحة الصنف المعني
- زر "عرض الكل" يؤدي لصفحة التقارير

#### 5.3 التحديث التلقائي
- استعلام كل 5 دقائق (بـ `refetchInterval` في React Query)

### معايير القبول
- [ ] الجرس يظهر عدد التنبيهات النشطة
- [ ] القائمة المنسدلة تعرض التفاصيل
- [ ] روابط التنبيهات تعمل
- [ ] يتحدث تلقائياً كل 5 دقائق
- [ ] TypeScript typecheck يمر

### الـ APIs المستخدمة
```
GET /api/alerts   ← قائمة التنبيهات النشطة
```

### الملفات التي ستُعدَّل
```
artifacts/web/src/components/header.tsx   ← إضافة جرس التنبيهات
```

### نتائج الاختبار
> *تُملأ بعد التنفيذ*

| الاختبار | النتيجة | ملاحظة |
|---|---|---|
| عرض عدد التنبيهات | — | |
| القائمة المنسدلة | — | |
| التحديث التلقائي | — | |
| TypeScript typecheck | — | |

### توثيق التنفيذ
> *يُضاف بعد الانتهاء*

---

## المرحلة 6 — جدول Audit Log واستيراد Excel

**الأولوية:** 🟢 منخفضة-متوسطة  
**الحالة:** ⏳ في الانتظار  
**التعقيد:** متوسط  
**الوقت المتوقع:** جلسة واحدة

### الوصف
إضافة جدول `audit_log` لتسجيل كل عملية في النظام، واستيراد بيانات المخزون الأولي من ملف Excel الموجود في المشروع.

### المتطلبات التفصيلية

#### 6.1 جدول Audit Log
- **Schema:** `id، user_id، user_name_snap، action، entity_type، entity_id، details (jsonb)، ip_address، created_at`
- تسجيل تلقائي لكل عمليات: إضافة/تعديل/حذف مادة أو تجهيز، إضافة عملية دخول/خروج، تسجيل دخول/خروج، تغيير صلاحيات المستخدمين
- لا يُحذف أبداً (append-only)

#### 6.2 استيراد Excel
- قراءة ملف `عهدة_مستودع_منظومة_الإسعاف_والطوارئ_بدمشق.xlsx`
- استخراج: الصنف، الكمية، التصنيف، الوحدة
- إدراج كـ "رصيد افتتاحي" (`type: "init"`) في جدول transactions
- تقرير بعدد الأصناف المستوردة بنجاح والأخطاء

### معايير القبول
- [ ] جدول audit_log يُنشأ في قاعدة البيانات
- [ ] كل العمليات الرئيسية تُسجَّل تلقائياً
- [ ] استيراد Excel يعمل ويُنشئ سجلات transactions
- [ ] TypeScript typecheck يمر

### الملفات التي ستُعدَّل/تُنشأ
```
lib/db/src/schema/audit-log.ts                    ← Schema الجديد
artifacts/api-server/src/middlewares/audit.ts     ← Middleware التسجيل
scripts/import-excel.mjs                          ← سكريبت استيراد Excel
```

### نتائج الاختبار
> *تُملأ بعد التنفيذ*

| الاختبار | النتيجة | ملاحظة |
|---|---|---|
| جدول audit_log موجود في قاعدة البيانات | — | |
| تسجيل عملية إدخال في audit_log | — | |
| تسجيل تسجيل الدخول في audit_log | — | |
| استيراد Excel وإنشاء السجلات | — | |
| TypeScript typecheck | — | |

### توثيق التنفيذ
> *يُضاف بعد الانتهاء*

---

## المرحلة 7 — المراجعة الشاملة والصقل النهائي والنشر

**الأولوية:** 🟢 ختامية  
**الحالة:** ⏳ في الانتظار  
**التعقيد:** متوسط  
**الوقت المتوقع:** جلسة واحدة

### الوصف
مراجعة شاملة لجميع الشاشات، إصلاح أي مشاكل بصرية أو وظيفية، وتجهيز النظام للنشر على شبكة داخلية.

### المتطلبات التفصيلية

#### 7.1 مراجعة التوحيد البصري
- التأكد من اتساق الألوان، الخطوط، المسافات في جميع الصفحات
- اختبار Dark Mode في كل الشاشات
- اختبار الاتجاه RTL في جميع المكونات
- مراجعة رسائل الخطأ والحالات الفارغة (Empty States)

#### 7.2 اختبارات وظيفية شاملة
- اختبار كل المسارات والنماذج
- اختبار صلاحيات كل دور (admin، warehouse_manager، viewer)
- اختبار حالات الحافة: كمية صفر، أرقام سالبة، نصوص طويلة جداً

#### 7.3 تحسينات الأداء
- التحقق من عدم وجود استعلامات غير ضرورية
- إضافة loading states لجميع النماذج
- إضافة disabled states للأزرار أثناء الإرسال

#### 7.4 تجهيز النشر
- مراجعة `replit.md` وتحديثه
- اختبار `pnpm run build` نظيف
- نشر على Replit مع إعداد متغيرات البيئة للإنتاج

#### 7.5 تغيير كلمة المرور الافتراضية
- تغيير `admin / Admin@1234` لكلمة مرور آمنة
- توثيق ذلك للمستخدم

### معايير القبول
- [ ] `pnpm run typecheck` بدون أخطاء
- [ ] `pnpm run build` بدون أخطاء
- [ ] كل الصفحات تعمل في Light وDark Mode
- [ ] جميع الأدوار تعمل بصلاحياتها الصحيحة
- [ ] النشر يعمل ويمكن الوصول إليه

### نتائج الاختبار
> *تُملأ بعد التنفيذ*

| الاختبار | النتيجة | ملاحظة |
|---|---|---|
| typecheck نظيف | — | |
| build ناجح | — | |
| Dark Mode لكل الصفحات | — | |
| صلاحيات admin | — | |
| صلاحيات warehouse_manager | — | |
| صلاحيات viewer | — | |
| النشر والوصول | — | |

### توثيق التنفيذ
> *يُضاف بعد الانتهاء*

---

## سجل التغييرات

| التاريخ | المرحلة | الإجراء | الحالة |
|---|---|---|---|
| 3 أغسطس 2026 | — | إنشاء الملف وتوثيق الخطة | ✅ |
| — | 1 | واجهة العمليات والسند | ⏳ |
| — | 2 | صفحة التقارير | ⏳ |
| — | 3 | إدارة المستخدمين | ⏳ |
| — | 4 | الإعدادات | ⏳ |
| — | 5 | نظام التنبيهات | ⏳ |
| — | 6 | Audit Log + استيراد Excel | ⏳ |
| — | 7 | مراجعة شاملة ونشر | ⏳ |

---

## ملاحظات تقنية مهمة

### الـ Hooks الجاهزة للاستخدام
كل الـ hooks مولَّدة مسبقاً من OpenAPI spec وجاهزة في `lib/api-client-react/src/generated/`:
```typescript
// العمليات
useListTransactions, useCreateTransactionIn, useCreateTransactionOut
useGetTransactionById, useGetTransactionPrint

// التقارير
useGetReportsStock, useGetReportsMovements, useGetReportsExpiry
useGetReportsBelowMin, useGetReportsEquipment

// التنبيهات
useGetAlerts

// المستخدمون
useListUsers, useCreateUser, useUpdateUser, useDeleteUser

// الإعدادات
useGetSettings, useUpdateSettings, useChangePassword
```

### مكتبات متاحة (لا تحتاج تثبيت)
- `recharts` — للمخططات
- `react-hook-form` + `@hookform/resolvers` + `zod` — للنماذج
- `date-fns` — لتنسيق التواريخ
- `lucide-react` — للأيقونات
- `sonner` — لإشعارات Toast
- `shadcn/ui` كاملاً — جميع المكونات

### قاعدة مهمة للطباعة
```css
/* يُضاف في كل صفحة تدعم الطباعة */
@media print {
  .no-print { display: none !important; }
  body { direction: rtl; font-family: 'Cairo', sans-serif; }
}
```
